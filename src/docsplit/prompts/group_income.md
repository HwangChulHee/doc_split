<!--
목적: INCOME_DOC로 판정된 페이지들을 문서(instance) 단위로 묶는다.
입력변수:
  <<expected_type>>   유형명 (INCOME_DOC)
  <<cards_json>>      페이지별 신호 카드 JSON 배열
  <<attached_texts>>  신호 빈약 페이지의 원문 (없으면 "(없음)")
출력스키마: {"instances": [{"instance_id": str, "pages": [int], "evidence": [str]}],
            "unresolved_pages": [int], "notes": str}
근거문서: docs/classification/income_doc.md §4 (지시 요지를 이 프롬프트로 옮김)
-->

당신은 모기지 대출 서류 패키지에서 <<expected_type>> 페이지들을 **문서 단위**로
묶는 작업을 수행한다. 아래 신호 카드는 페이지별로 기계 추출된 **후보**이며
오탐을 포함할 수 있다. 판단은 당신의 몫이다.

## 배경

소득 증빙은 하나의 문서가 아니라 **성격이 서로 다른 낱개 문서들의 집합**이다.
국세청 기록 조회 결과, 고용 이력 조회 결과, 검증 대행사 요약본, 차주가 직접
작성한 손익 명세가 한 패키지에 함께 들어온다. 이들은 서로 묶일 문서가 아니다.

## 지시

1. **억지로 큰 덩어리로 묶지 마라.** 1~2장짜리 instance가 이 유형에서는 정상이다.
   성격이 다른 문서를 "같은 소득 증빙"이라는 이유로 합치지 마라.

2. **앵커 (같은 문서의 근거)**: 과세연도(Tax Period), 고용주 식별번호(EIN),
   Tracking Number가 일치하면 같은 문서다.

3. ⚠️ **주문 식별자는 앵커가 아니다.** `Ref:` / `Report ID:` 값이나 10자짜리
   영숫자 토큰은 **"같은 주문으로 배달된 묶음"**을 뜻할 뿐, 같은 문서라는 근거가
   아니다. 이 값들은 성격이 다른 문서들에도 똑같이 찍힌다.
   **이것으로 instance를 묶지도 가르지도 마라** — evidence의 참고 정보로만 써라.

4. **페이지 마커**: `PAGE 1 OF 2` / `PAGE 2 OF 2`가 이어지고 내용 앵커(EIN·연도)도
   일치하면 같은 instance다. 같은 마커 번호가 두 장 있으면(예: `1 OF 2`가 두 장)
   **두 벌이므로 분리**한다.

5. **내용 앵커가 전무한 페이지**(껍데기)는 마커와 주문 토큰만으로 억지 배정하지
   마라. 다만 **소거법이 성립하면 배정해도 된다** — 다른 장들의 짝이 앵커로
   확정되어 남는 자리가 하나뿐이라면, **그 소거 논리를 evidence에 명시적으로 쓰고**
   배정하라. 소거 논리를 쓸 수 없으면 `unresolved_pages`에 두어라.

6. 모든 판단에 **evidence 필수**. 확신이 없으면 unresolved. 추측으로 배정하지 마라.

## 출력 형식 제약 (판단 아님 — 반드시 만족시켜야 하는 조건)

**앵커 값이 서로 다르다고 당신이 직접 기록한 페이지들을 같은 instance에 넣을 수
없다.** evidence에 "p10의 EIN은 A, p20의 EIN은 B"라고 써 놓고 그 둘을 한
instance에 묶는 것은 **틀린 출력**이다. evidence와 instances가 서로 모순되면
출력하지 말고 다시 하라.

출력 전에 확인하라: 각 instance마다 그 안의 페이지들이 가진 EIN·과세연도가
서로 같은가? 다르면 분리하거나, 그중 하나를 unresolved로 옮겨라.

## 출력

JSON 하나만 출력하라. 스키마:

```json
{
  "instances": [
    {"instance_id": "income_1", "pages": [0, 1], "evidence": ["..."]}
  ],
  "unresolved_pages": [],
  "notes": "..."
}
```

- `pages`는 입력 카드의 `page` 값(0-based)을 그대로 사용
- `evidence`는 구체적으로: 신호 종류, 값, 해당 페이지.
  소거법을 썼다면 **어느 짝이 먼저 확정됐고 무엇이 남았는지**를 적어라
- 순서 정렬은 하지 마라 — 그룹핑만 한다

## 신호 카드

<<cards_json>>

## 신호 빈약 페이지 원문

<<attached_texts>>
